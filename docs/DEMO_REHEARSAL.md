# Five-minute demo rehearsal

1. Open invoice intake and point out the provider privacy boundary.
2. Upload `happy.pdf`; show the approval explanation, inline source locations, PO matching, controls, ledger ID, and PDF preview.
3. Upload `duplicate.pdf`; show `DUPLICATE` and no ledger mutation.
4. Upload `missing_po.pdf`; show ranked candidates, confirm `PO-1002`, then show same-run posting.
5. Upload `bundle_unknown.pdf`; confirm the stored decomposition and show two component allocations.
6. Upload `tax_inclusive.pdf`; show `$590.00` observed gross becoming `$500.00` net plus `$90.00` tax in the observed-versus-accounting comparison.
7. Open Dashboard filters, refresh a run URL, then reset and confirm the baseline is restored.

These sample PDFs are test assets. Every rehearsal upload invokes Azure and the selected mapper, so use intentional credentials and budget for provider spend.
