# Five-minute demo rehearsal

1. Open the console and point out the synthetic-data and provider privacy boundary.
2. Run `happy`; show persisted stages, observed/normalized evidence, PO comparison, controls, ledger ID, and PDF preview.
3. Run `duplicate`; show `DUPLICATE` and no ledger mutation.
4. Run `missing_po`; show ranked candidates, confirm `PO-1002`, then show same-run posting.
5. Run `bundle_unknown`; confirm the stored decomposition and show two component allocations.
6. Run `tax_inclusive`; show `$590.00` observed gross becoming `$500.00` net plus `$90.00` tax.
7. Open Dashboard filters, refresh a run URL, then reset and confirm the baseline is restored.

Use recorded mode for rehearsal so Azure, OpenAI, and Gemini receive no requests.
